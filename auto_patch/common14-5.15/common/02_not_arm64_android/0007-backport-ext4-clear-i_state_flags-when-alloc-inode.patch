From 5f59d06ef6438ee3684ef7b040fd80aa1f71804a Mon Sep 17 00:00:00 2001
From: Jiucheng Xu <jiucheng.xu@amlogic.com>
Date: Fri, 30 Jan 2026 14:16:49 +0800
Subject: [PATCH] backport: ext4: clear i_state_flags when alloc inode

commit 4091c8206cfd2e3bb529ef260887296b90d9b6a2 upstream.

i_state_flags used on 32-bit archs, need to clear this flag when
alloc inode.
Find this issue when umount ext4, sometimes track the inode as orphan
accidently, cause ext4 mesg dump.

Change-Id: I453d4cfe4b426745c5cf291c6ed6930320d4ae9b
Signed-off-by: Jiucheng Xu <jiucheng.xu@amlogic.com>
---
 fs/ext4/ialloc.c | 2 ++
 fs/ext4/inode.c  | 2 ++
 fs/ext4/super.c  | 3 +++
 3 files changed, 7 insertions(+)

diff --git a/fs/ext4/ialloc.c b/fs/ext4/ialloc.c
index 5841686e80b3..4435a591b62b 100644
--- a/fs/ext4/ialloc.c
+++ b/fs/ext4/ialloc.c
@@ -1301,7 +1301,9 @@ struct inode *__ext4_new_inode(struct user_namespace *mnt_userns,
 					      sizeof(gen));
 	}
 
+#ifndef CONFIG_AMLOGIC_EXT4_FIX1
 	ext4_clear_state_flags(ei); /* Only relevant on 32-bit archs */
+#endif
 	ext4_set_inode_state(inode, EXT4_STATE_NEW);
 
 	ei->i_extra_isize = sbi->s_want_extra_isize;
diff --git a/fs/ext4/inode.c b/fs/ext4/inode.c
index e091df200962..9ffa5c2231f8 100644
--- a/fs/ext4/inode.c
+++ b/fs/ext4/inode.c
@@ -4761,7 +4761,9 @@ struct inode *__ext4_iget(struct super_block *sb, unsigned long ino,
 	ei->i_projid = make_kprojid(&init_user_ns, i_projid);
 	set_nlink(inode, le16_to_cpu(raw_inode->i_links_count));
 
+#ifndef CONFIG_AMLOGIC_EXT4_FIX1
 	ext4_clear_state_flags(ei);	/* Only relevant on 32-bit archs */
+#endif
 	ei->i_inline_off = 0;
 	ei->i_dir_start_lookup = 0;
 	ei->i_dtime = le32_to_cpu(raw_inode->i_dtime);
diff --git a/fs/ext4/super.c b/fs/ext4/super.c
index 97dd2228b04d..77619ac11971 100644
--- a/fs/ext4/super.c
+++ b/fs/ext4/super.c
@@ -1292,6 +1292,9 @@ static struct inode *ext4_alloc_inode(struct super_block *sb)
 
 	inode_set_iversion(&ei->vfs_inode, 1);
 	ei->i_flags = 0;
+#ifdef CONFIG_AMLOGIC_EXT4_FIX1
+	ext4_clear_state_flags(ei);	/* Only relevant on 32-bit archs */
+#endif
 	spin_lock_init(&ei->i_raw_lock);
 	INIT_LIST_HEAD(&ei->i_prealloc_list);
 	atomic_set(&ei->i_prealloc_active, 0);
-- 
2.52.0

